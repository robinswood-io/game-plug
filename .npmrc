# Force workspaces usage
workspaces=true

# Install all dependencies (dev + prod)
# Important for monorepo builds
production=false

# Prevent auto-install in subdirectories
prefer-workspace-packages=true

# Prevent npm from trying to install in individual workspace dirs
workspace-concurrency=1
