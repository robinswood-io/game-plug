# NPM Registry - Utilise le registry public par défaut
registry=https://registry.npmjs.org/

# Scoped packages locaux (file: protocol)
@workspace:registry=https://registry.npmjs.org/
@robinswood:registry=https://registry.npmjs.org/

# Optionnel: timeout et retry
fetch-timeout=60000
fetch-retries=3
